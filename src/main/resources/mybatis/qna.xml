<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.mvc.qna.QnaDAOInter">
   <!-- QnA 등록 -->
  <insert id="create" parameterType="dev.mvc.qna.QnaVO">
    INSERT INTO qna(qnano, memberno, title, content, passwd, word, rdate)
    VALUES(qna_seq.nextval, #{memberno}, #{title}, #{content}, #{passwd}, #{word}, sysdate)
  </insert>
  
  <!-- 모든 카테고리의 등록된 글목록 -->
  <select id="list_all" resultType="dev.mvc.qna.QnaVO">
    SELECT qnano, memberno, title, content, passwd, rdate, word
    FROM qna 
    ORDER BY qnano DESC
  </select> 

    <!-- 일반적인 조회 -->
  <select id="read" resultType="dev.mvc.qna.QnaVO" parameterType="int">
    SELECT qnano, memberno, title, content, passwd, rdate, word
    FROM qna
    WHERE qnano = #{qnano}
  </select>

   <!-- 패스워드 확인 -->
  <select id="password_check" resultType="int" parameterType="dev.mvc.qna.QnaVO">
    SELECT COUNT(*) as cnt
    FROM qna
    WHERE qnano = #{qnano} AND passwd = #{passwd}
  </select>
  
    <!-- 글 수정 -->
  <update id="update_text" parameterType="dev.mvc.qna.QnaVO">
    UPDATE qna
    SET title=#{title}, content=#{content}, word=#{word}
    WHERE qnano = #{qnano}
  </update>
  
   <!-- 삭제 -->
  <delete id="delete" parameterType="int">
    DELETE FROM qna
    WHERE qnano=#{qnano}
  </delete>

    



</mapper>